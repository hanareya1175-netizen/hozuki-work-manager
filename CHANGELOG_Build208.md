# HozukiWorks Ver2.0.0 Build208

## NEW表示の根本修正

- NEW状態を画面上の行位置ではなく、募集番号（recruit_id）だけで管理。
- スマホではNEWと募集内容を同一ボタン内に表示し、展開時の行高変化による表示ずれを防止。
- 詳細ボタンのコールバック内で既読を保存してから再描画するため、押した募集だけNEWと件数が直ちに減る。
- 詳細展開中は並び順だけを固定し、NEW状態は常にrecruit_viewsから再計算。
- 既読登録は冪等処理のため、同じ募集を繰り返し開いても重複記録しない。
